# ZCode Task Notifier 版本路线图

## 版本边界

- **第一版（当前发布版）**：先完成当前既定功能，完成验证后单独提交并推送远端。第一版发布稳定后，立即按本文件建立第二版、第三版的实施计划并继续实施，不再等待额外确认。
- **第二版**：按下列功能顺序逐项实施。每完成一项就单独提交，并立即推送远端；不得把多项功能合并成一个提交。
- **第三版**：只实施“从微信 ClawBot 同意计划审批并启动对应会话”这一项，完成后单独提交并立即推送远端。
- 当前明确取消“失败后自动重发/自动重试”行为；后续版本不得重新引入该行为。

## 第二版功能清单

### 1. 扩展任务状态占位

通知必须对应实际任务的停顿节点：计划待审批、方案待用户选择、任务最终完成或失败。初始化时确认 Harness/规则、表示已就绪或等待用户提供第一项任务，不属于任务完成；普通进度、继续执行中的阶段结果也不通知。不能仅凭模型回复结束或 `task_complete` 事件就判定为可通知，必须结合本回合任务输入、结构化停顿信号与最终输出。待用户选择应与计划审批保留不同原因，供第三版绑定审批时使用。

将现有只有“完成”和“失败”的状态展示/传递扩展为以下三种明确状态：

- **计划待审批**：AI 已返回执行计划，但尚未收到同意信号；不能误标为完成或失败，也不能自动开始执行。
- **完成**：任务已经正常结束。
- **失败**：任务执行失败，并保留可追溯的失败结果；不得因此自动重发。

验收要求：状态值、展示文案、通知内容和状态流转语义一致；待审批状态必须可与完成、失败区分，并为第三版审批入口保留稳定关联信息。

### 2. ZCode 标识与 Agent 扩展占位

- ZCode 任务/通知前增加 `[zcode]` 标识，保证用户能识别任务来源。
- 为后续 Claude Code、DSH 适配保留清晰的扩展占位和边界，但第二版不伪实现这两类 Agent，也不改变其协议或配置。

验收要求：ZCode 标识在现有任务通知链路中稳定出现；扩展占位不影响 ZCode 现有行为，未适配的 Agent 不得被误报为已支持。

### 3. 超量自动清理 ZCode 历史

对 `[agent名字]` 类自动化任务执行数量阈值控制：当同一匹配规则下的数量**超过十条**时，自动清理 ZCode 历史记录。

安全边界：只允许清理能够确认属于本产品、符合匹配规则且已经进入完成或失败等终态的历史记录；不得清理待审批、执行中、未确认归属、其他 Agent、其他产品或无法准确匹配的记录。清理应可审计、可重复执行且不误删，至少保留阈值内的最新合格记录。

验收要求：阈值严格按“超过十条”触发；边界值、混合来源、非终态和匹配失败场景均有测试；清理结果和跳过原因可追溯。

### 4. 微信 ClawBot `/刷新` 类指令只刷新 iLink 上下文

用户通过微信向 ClawBot 发送指定控制消息（例如 `/刷新`）时，只刷新 iLink 的 `context_token`，不触发模型对话、不产生新的自动回复任务，也不执行失败重发。

实施门槛：先调查并复用原版已有的同类原生能力；只有确认原版没有可用能力时，才尝试增加外置链路。外置实现必须限定在现有边界内，不能借机修改 ZCode 本体。

验收要求：控制消息只导致 `context_token` 刷新；模型调用、对话上下文生成和任务派发均不发生；非控制消息仍保持原行为；重复刷新、刷新失败和并发刷新有明确结果且不泄露凭据。

## 第三版功能清单（仅此一项）

### 微信 ClawBot 同意计划审批并启动对应会话

用户在微信 ClawBot 发送“同意”后，系统向唯一绑定的对应 Agent、对应 session 中的待审批计划发送同意信号，使该计划直接开始执行。

安全与正确性要求：

- 同意消息必须绑定到唯一的 Agent、session 和当前未过期的待审批计划；无法唯一匹配、计划已变化或已过期时拒绝启动。
- 校验 ClawBot 消息来源和用户权限，防止未授权代批；同意信号必须防重放、防重复启动，并避免把普通聊天中的“同意”误当成审批。
- 只允许从“计划待审批”进入执行，完成、失败、执行中及其他状态不得重复启动；启动失败要记录失败原因，不自动重发。
- 留存最小必要审计信息（关联关系、授权结果、时间、状态变化和启动结果），不得在微信消息、日志或错误信息中泄露计划正文、凭据、context token 或其他敏感数据。

实施门槛：先确认原版是否提供受控的审批接口；只有在有实证且权限边界可控时才调用原生接口，否则采用不修改 ZCode 本体的外置链路，并补齐唯一绑定、鉴权、过期、防重放、防误批和审计验证。

## 通用实施与发布规则

1. 每项功能开始前先核对现有实现、调用链、原版能力和数据归属，形成可复核的实施计划。
2. 每项功能完成后执行与其风险匹配的静态检查、单元/集成测试和安全检查；未完成或验证不足不得标记为完成。
3. 功能级提交使用中文提交说明，写集只包含该功能及其必要测试/文档；提交后立即推送远端，并核对远端已到达。若推送失败，明确记录为未推送，不得冒充已发布。
4. 第一版和第二版继续遵循“不修改 ZCode 本体”的外置链路原则；只有后续实证表明原生审批接口必须受控调用时，第三版才允许在最小范围接入该接口。
5. 所有版本均不得写入真实账号、令牌、session 标识或其他本机/环境敏感信息；测试使用隔离的虚拟数据。
